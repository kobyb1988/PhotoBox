<UserControl x:Class="ImageMaker.AdminView.Controls.PasswordPromptCtl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ImageMaker.AdminViewModels.ViewModels;assembly=ImageMaker.AdminViewModels"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:behaviors="clr-namespace:ImageMaker.CommonView.Behaviors;assembly=ImageMaker.CommonView"
             mc:Ignorable="d" 
             Background="{StaticResource BackgroundBrush}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid d:DataContext="{d:DesignInstance viewModels:PasswordPromptViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Label Content="Введите пароль: " VerticalAlignment="Center" HorizontalAlignment="Right"/>
        <PasswordBox Grid.Column="1" Grid.Row="0" x:Name="PwBox">
            <i:Interaction.Behaviors>
                <behaviors:PasswordBehavior />
            </i:Interaction.Behaviors>
        </PasswordBox>
        <Button Grid.Row="1" Grid.Column="1" Content="ОК" Command="{Binding Path=SubmitCommand}" 
                CommandParameter="{Binding ElementName=PwBox, Path=SecurePassword}"/>
    </Grid>
</UserControl>
