<UserControl x:Class="SandBox.Views.PagerTestCtl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:customControls="clr-namespace:ImageMaker.Themes.CustomControls;assembly=ImageMaker.Themes"
             xmlns:viewModels="clr-namespace:SandBox.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <UserControl.Resources>
        <viewModels:TestViewModel x:Key="ViewModel"/>
    </UserControl.Resources>
    <Border Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <!--<customControls:DataPagerCtl Margin="10" 
                                         PageSize="5"
                                         PagesInRow="3"
                                         ItemsSource="{Binding Source={StaticResource ViewModel}, Path=Items}" Background="Red" Grid.Row="1"/>-->
        </Grid>
    </Border>
    
</UserControl>
